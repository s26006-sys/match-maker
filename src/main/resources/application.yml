spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hkt?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: ${MATCHMAKER_DB_PASSWORD} # ???? ??? ??? ?? ??
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true